# 무인 자동 코인거래 시스템 실행 계획서

> 목표: 사용자의 개입 없이 Claude가 업비트(또는 빗썸)에서 매수/매도 거래를 자동으로 수행한다.
> 입출금(원화·코인)은 사용자가 직접 하고, 시스템은 **거래(조회+주문)만** 담당한다.

---

## 1. 핵심 원칙

1. **출금 권한 제로**: API 키에 출금 권한을 절대 부여하지 않는다. 시스템이 해킹당해도 자산이 외부로 나갈 수 없는 구조가 1차 방어선이다.
2. **손실 한도 우선**: 수익 전략보다 손실 차단 장치(킬스위치)를 먼저 만든다.
3. **단계적 확대**: 페이퍼 트레이딩 → 소액 실전(최소주문 수준) → 점진적 증액. 어떤 단계도 건너뛰지 않는다.
4. **모든 거래 기록**: 주문·체결·잔고·판단 근거를 전부 로그로 남겨 언제든 복기할 수 있게 한다.

---

## 2. 거래소 선택: 업비트 권장

| 항목 | 업비트 | 빗썸 |
|------|--------|------|
| API 방식 | Open API (JWT 인증), 문서화 우수 | Open API 2.0 (업비트 호환 형태로 개편됨) |
| 시세 조회 | REST + WebSocket, 인증 불필요 | REST + WebSocket |
| 주문 API | 시장가/지정가/최유리 지원 | 시장가/지정가 지원 |
| 최소 주문 | 5,000원 | 5,000원 |
| 수수료(일반) | 0.05% | 0.04% (쿠폰 적용 시 더 낮음) |
| IP 화이트리스트 | 지원(필수 설정) | 지원 |
| 라이브러리 생태계 | `pyupbit` 등 성숙 | `pybithumb` 등 존재하나 상대적으로 빈약 |

**결론**: 개발 안정성과 문서/커뮤니티 성숙도에서 업비트가 유리하다. 빗썸은 수수료가 약간 낮지만 첫 구축은 업비트로 하고, 인터페이스를 추상화해 두면 빗썸 추가는 어댑터 하나로 가능하다.

---

## 3. 시스템 아키텍처

Claude 세션은 상시 실행되는 프로세스가 아니므로, **"실행 엔진은 코드가 24시간 담당, 전략 판단·튜닝은 Claude가 주기적으로 담당"** 하는 2계층 구조로 설계한다.

```
┌────────────────────────────────────────────────────┐
│  계층 1: 실행 엔진 (Python, 24시간 상시 실행)         │
│  - 시세 수신(WebSocket) / 잔고·주문 관리             │
│  - config.yaml에 정의된 전략 파라미터대로 기계적 실행   │
│  - 리스크 가드(손실 한도, 포지션 한도) 강제            │
│  - 모든 활동을 로그/DB에 기록                        │
└──────────────────────┬─────────────────────────────┘
                       │ 설정 파일 + 거래 로그 (git)
┌──────────────────────┴─────────────────────────────┐
│  계층 2: Claude (스케줄 트리거로 주기 실행, 예: 1시간)  │
│  - 거래 로그·시장 상황 분석                          │
│  - 전략 파라미터 조정(config 수정 → 커밋)             │
│  - 이상 감지 시 엔진 정지 플래그 설정                 │
│  - 일일 리포트 생성                                  │
└────────────────────────────────────────────────────┘
```

이렇게 나누는 이유:
- 코인 시장은 24시간이고 초 단위 대응(주문 체결, 손절)은 상시 프로세스만 가능하다.
- Claude를 매 주문마다 호출하면 지연·비용이 크고, 세션이 항상 떠 있다는 보장이 없다.
- 반대로 순수 규칙 기반 봇은 시장 국면 변화에 스스로 적응하지 못한다 → Claude가 그 역할을 맡는다.

### 실행 엔진 호스팅 옵션

| 옵션 | 장점 | 단점 | 권장도 |
|------|------|------|--------|
| 저가 VPS (예: 국내/일본 리전) | 24시간 안정, IP 고정(화이트리스트에 유리) | 월 비용(~1만원 미만), 초기 셋업 | ★★★ 권장 |
| 집 PC/미니PC 상시 가동 | 무료 | 정전·재부팅 리스크, IP 변동 가능 | ★★ |
| GitHub Actions cron | 무료, 관리 불필요 | 최소 5분 간격+지연 심함, IP 유동적(화이트리스트 불가) | ★ 비권장 |

**권장**: 고정 IP를 가진 저가 VPS. 업비트 API 키는 등록된 IP에서만 동작하므로 고정 IP가 사실상 필수다.

---

## 4. 보안 설계

1. **API 키 권한**: `자산 조회` + `주문` 만 체크. **출금 권한 미부여** (업비트는 Open API에서 출금 권한을 별도 신청해야 하므로 기본적으로 안전).
2. **IP 화이트리스트**: VPS 고정 IP만 등록. 다른 IP에서의 요청은 거래소가 차단.
3. **키 보관**:
   - VPS: 환경변수 또는 `.env` (파일 권한 600), 절대 git에 커밋하지 않음 (`.gitignore` 등록).
   - 레포: GitHub Secrets 사용 (Claude가 전략 조정 시 키에 접근할 필요 없음 — 키는 엔진만 사용).
4. **키 로테이션**: 90일마다 재발급 (업비트 키 유효기간 1년, 만료 알림 설정).
5. **거래 상한**: 엔진 레벨에서 1회 주문 금액 상한, 1일 누적 주문 금액 상한을 하드코딩 수준으로 강제.

---

## 5. 전략 설계 (초기 → 확장)

### 5.1 1차 전략: 변동성 돌파 (래리 윌리엄스 방식, 검증된 단순 전략)
- 매일 09:00(업비트 일봉 기준) 리셋.
- 목표가 = 당일 시가 + (전일 고가 − 전일 저가) × k (k=0.5 시작).
- 현재가가 목표가 돌파 시 시장가 매수 → 다음날 09:00 전량 매도.
- 장점: 로직 단순, 백테스트 용이, 횡보장 손실 제한적.

### 5.2 2차 전략(병행 가능): 분할 DCA + 그리드
- 지정 코인(BTC, ETH 등 대형주 한정)에 대해 하락 시 분할 매수, 목표 수익률 도달 시 분할 매도.
- 횡보장에서 변동성 돌파의 약점을 보완.

### 5.3 Claude의 역할 (전략 튜닝 루프, 1시간~1일 주기)
- 최근 체결 로그와 시장 데이터(변동성, 추세 강도)를 분석.
- k값, 대상 코인 목록, 포지션 크기, 전략 on/off를 `config.yaml`로 조정.
- 조정 내역과 근거를 커밋 메시지 + 일일 리포트로 기록.
- **Claude가 직접 주문 API를 호출하는 모드**(옵션): 스케줄 트리거로 깨어난 세션이 시세를 보고 직접 매수/매도 판단·주문하는 것도 가능하다. 다만 최소 주기가 시간 단위라 스윙/포지션 트레이딩에만 적합하며, 손절 같은 즉각 대응은 여전히 엔진이 담당해야 한다.

### 5.4 백테스트
- `pyupbit`로 최근 2~3년 일봉/분봉 수집.
- 수수료(0.05%×2) + 슬리피지(0.1% 가정) 반영한 백테스트 필수.
- 지표: 누적수익률, MDD(최대낙폭), 승률, 손익비. **MDD > 20% 전략은 실전 투입 금지.**

---

## 6. 리스크 관리 (엔진에 하드코딩)

| 장치 | 규칙 (초기값) |
|------|---------------|
| 1회 주문 한도 | 총 운용자산의 10% 이하 |
| 코인당 최대 보유 | 총 운용자산의 25% 이하 |
| 일일 손실 한도 | −3% 도달 시 당일 신규 매수 전면 중단 |
| 누적 손실 킬스위치 | 총자산 −10% 도달 시 전량 매도 후 엔진 정지, 사용자 알림 |
| 주문 검증 | 주문 전 잔고 재확인, 체결 후 실제 체결가 기록 |
| API 장애 대응 | 연속 실패 5회 시 신규 주문 중단, 재시도는 지수 백오프 |
| 이상 급변동 | 5분 내 ±10% 급변 시 신규 진입 금지 (상장폐지·해킹 뉴스 등 이벤트 방어) |

---

## 7. 모니터링 & 알림

- **로그**: 모든 주문/체결/판단을 SQLite + JSON 로그로 기록, 매일 git 커밋(키 제외).
- **일일 리포트**: Claude가 매일 아침 수익률·보유현황·전략 판단 근거를 요약해 기록 (텔레그램 봇 연동 시 푸시 가능).
- **긴급 알림**: 킬스위치 발동, API 오류 지속, 잔고 불일치 시 즉시 알림 (텔레그램 권장 — 무료, 구현 간단).
- **사용자 수동 개입 스위치**: `STOP` 파일 하나 만들면 엔진이 즉시 신규 주문을 멈추는 단순한 정지 장치.

---## 8. 단계별 로드맵

| 단계 | 내용 | 기간 | 통과 기준 |
|------|------|------|-----------|
| 0 | 사용자: 업비트 API 키 발급(조회+주문, IP 등록), VPS 준비 | 1일 | 키·서버 준비 완료 |
| 1 | 엔진 뼈대 구축: 시세 수신, 잔고 조회, 주문 모듈, 로그 | 2~3일 | 수동 테스트 주문 1건 성공 |
| 2 | 백테스트: 변동성 돌파 전략 파라미터 탐색 | 2~3일 | MDD<20%, 수수료 반영 후 + 수익 |
| 3 | **페이퍼 트레이딩**: 실주문 없이 가상 체결로 2주 운영 | 2주 | 백테스트 대비 괴리 원인 설명 가능 |
| 4 | **소액 실전**: 코인당 5,000~10,000원 주문으로 2주 | 2주 | 킬스위치·알림 정상 작동 확인 |
| 5 | 운용 확대 + Claude 튜닝 루프 가동 | 이후 | 월 단위 성과 리뷰 |

---

## 9. 사용자가 해야 할 일 (체크리스트)

- [ ] 업비트 마이페이지 → Open API 관리 → 키 발급 (**자산조회 + 주문만 체크, 출금 제외**)
- [ ] VPS 계약 후 고정 IP를 API 키에 등록
- [ ] 초기 운용 원화 입금 (소액 실전 단계: 10~30만원 권장)
- [ ] 텔레그램 알림 받을 봇 생성 (선택)
- [ ] 킬스위치 발동 시 연락 받으면 상황 확인

---

## 10. 유의사항 (법·세금·현실적 한계)

- **세금**: 국내 가상자산 투자소득 과세 시행 일정을 확인할 것 (유예 연장 이력 있음). 모든 거래 기록이 남으므로 신고 자료로 활용 가능.
- **약관**: 업비트·빗썸 모두 개인의 API 자동매매 자체는 허용. 단 초당 요청 제한(업비트: 주문 초당 8회, 조회 초당 30회 수준)을 준수해야 하며 위반 시 키가 차단될 수 있다.
- **현실적 기대치**: 자동매매는 "무조건 수익"이 아니라 "감정 배제 + 24시간 규칙 실행"이 본질이다. 하락장에서는 현금 보유가 최선일 수 있고, 그 판단도 시스템에 포함되어야 한다.
- **본인 명의**: API 키와 계좌는 반드시 사용자 본인 명의로만 운용한다 (타인 자금 운용은 불법).

---

## 11. 다음 단계

이 계획이 승인되면:
1. 레포에 엔진 뼈대(`engine/`), 전략(`strategies/`), 백테스트(`backtest/`), 설정(`config.yaml`) 구조로 코드 구현 시작.
2. 시세 조회·백테스트는 API 키 없이도 가능하므로 즉시 개발 가능.
3. 실주문 모듈은 사용자의 API 키 준비 완료 후 소액 테스트로 검증.
