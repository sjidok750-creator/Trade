# 빗썸 무인 자동 코인거래 시스템 실행 계획서

> 목표: 사용자의 개입 없이 Claude가 설계·운영하는 시스템이 **빗썸**에서 매수/매도 거래를 자동으로 수행한다.
> 입출금(원화·코인)은 사용자가 직접 하고, 시스템은 **거래(조회+주문)만** 담당한다.
> 빗썸은 사용자의 서브 거래소로 **소액 운용**에서 시작하며, 원금 보전을 최우선으로 복리 성장을 노린다.

---

## 1. 핵심 원칙

1. **출금 권한 제로**: API 키에 출금 권한을 절대 부여하지 않는다. 시스템이 해킹당해도 자산이 외부로 나갈 수 없는 구조가 1차 방어선이다.
2. **손실 한도 우선**: 수익 전략보다 손실 차단 장치(킬스위치)를 먼저 만든다. 소액 계좌일수록 한 번의 큰 손실이 치명적이다(−50% 손실은 +100% 수익이 있어야 복구된다).
3. **단계적 확대**: 페이퍼 트레이딩 → 소액 실전(최소주문 수준) → 점진적 증액. 어떤 단계도 건너뛰지 않는다.
4. **모든 거래 기록**: 주문·체결·잔고·판단 근거를 전부 로그로 남겨 언제든 복기할 수 있게 한다.
5. **정직한 기대치**: 자동매매의 본질은 "감정 배제 + 24시간 규칙 실행"이지 수익 보장이 아니다. 목표는 대박이 아니라 **꾸준한 복리**다. 소액에서 검증된 시스템만이 증액할 자격을 얻는다.

---

## 2. 빗썸 API 환경

빗썸은 2024년부터 **Open API 2.0**(JWT 인증, 업비트와 유사한 구조)을 제공하며 자동매매 구축에 충분하다.

| 항목 | 내용 |
|------|------|
| 인증 | API 2.0 — JWT(액세스 키 + 시크릿 키), 구버전 1.0(HMAC)도 병행 제공 |
| 시세 조회 | REST(인증 불필요) + WebSocket 실시간 시세 |
| 주문 | 지정가/시장가 지원 (`/v1/orders`) |
| 요청 제한 | 공개 API·개인 API 모두 초당 수십 회 수준 — 전략상 충분. 정확한 현행 한도는 구현 시 공식 문서에서 확정 |
| 수수료 | 기본 0.25%지만 **무료 수수료 쿠폰 적용 시 0.04%** — 운영 전 반드시 쿠폰 적용 (왕복 0.5% vs 0.08%는 소액 전략의 생사를 가른다) |
| 최소 주문 | 원화 마켓 기준 소액(코인별 상이) — 구현 시 마켓 정보 API로 코인별 최소 주문 금액을 조회해 하드코딩 없이 검증 |
| IP 등록 | API 키 발급 시 사용 IP 등록 — 고정 IP 서버 필요 |
| 라이브러리 | `python-bithumb` 등 API 2.0 대응 라이브러리 존재. 신뢰성을 위해 **공식 REST를 직접 얇게 감싼 자체 클라이언트**를 구현하는 것을 기본으로 한다 |

**설계 방침**: 거래소 접근을 `Exchange` 인터페이스로 추상화한다. 빗썸 API 2.0이 업비트와 구조가 유사하므로, 나중에 업비트(주 거래소)로 확장할 때 어댑터 추가만으로 동일 전략을 돌릴 수 있다.

---

## 3. 시스템 아키텍처

Claude 세션은 상시 실행되는 프로세스가 아니므로, **"실행 엔진은 코드가 24시간 담당, 전략 판단·튜닝은 Claude가 주기적으로 담당"** 하는 2계층 구조로 설계한다.

```
┌────────────────────────────────────────────────────┐
│  계층 1: 실행 엔진 (Python, 24시간 상시 실행)         │
│  - 빗썸 시세 수신(WebSocket) / 잔고·주문 관리         │
│  - config.yaml에 정의된 전략 파라미터대로 기계적 실행   │
│  - 리스크 가드(손실 한도, 포지션 한도) 강제            │
│  - 모든 활동을 로그/DB에 기록                        │
└──────────────────────┬─────────────────────────────┘
                       │ 설정 파일 + 거래 로그 (git)
┌──────────────────────┴─────────────────────────────┐
│  계층 2: Claude (스케줄 트리거로 주기 실행, 예: 1시간)  │
│  - 거래 로그·시장 상황 분석                          │
│  - 전략 파라미터 조정(config 수정 → 커밋)             │
│  - 이상 감지 시 엔진 정지 플래그 설정                 │
│  - 일일 리포트 생성                                  │
└────────────────────────────────────────────────────┘
```

이렇게 나누는 이유:
- 코인 시장은 24시간이고 초 단위 대응(주문 체결, 손절)은 상시 프로세스만 가능하다.
- Claude를 매 주문마다 호출하면 지연·비용이 크고, 세션이 항상 떠 있다는 보장이 없다.
- 반대로 순수 규칙 기반 봇은 시장 국면 변화에 스스로 적응하지 못한다 → Claude가 그 역할을 맡는다.

### 실행 엔진 호스팅 옵션

| 옵션 | 장점 | 단점 | 권장도 |
|------|------|------|--------|
| 저가 VPS (국내/일본 리전) | 24시간 안정, 고정 IP(빗썸 IP 등록에 필수적) | 월 비용(~1만원 미만), 초기 셋업 | ★★★ 권장 |
| 집 PC/미니PC 상시 가동 | 무료 | 정전·재부팅 리스크, IP 변동 가능 | ★★ |
| GitHub Actions cron | 무료, 관리 불필요 | 최소 5분 간격+지연 심함, IP 유동적(IP 등록 불가) | ★ 비권장 |

**권장**: 고정 IP를 가진 저가 VPS. 소액 운용이라 서버비가 수익을 갉아먹지 않도록 최저가 플랜(월 5천원 내외)으로 충분하다.

---

## 4. 보안 설계

1. **API 키 권한**: 빗썸 API 관리에서 `자산 조회` + `주문` 만 허용. **출금 권한 미부여**.
2. **IP 등록**: VPS 고정 IP만 등록. 다른 IP에서의 요청은 거래소가 차단.
3. **키 보관**:
   - VPS: 환경변수 또는 `.env` (파일 권한 600), 절대 git에 커밋하지 않음 (`.gitignore` 등록).
   - 레포: GitHub Secrets 사용 (Claude가 전략 조정 시 키에 접근할 필요 없음 — 키는 엔진만 사용).
4. **키 로테이션**: 90일마다 재발급, 만료 알림 설정.
5. **거래 상한**: 엔진 레벨에서 1회 주문 금액 상한, 1일 누적 주문 금액 상한을 하드코딩 수준으로 강제.

---

## 5. 전략 설계 — 소액 계좌 최적화

소액 계좌의 제약: (a) 수수료·슬리피지 비중이 크다 (b) 분산할수록 주문이 최소 금액에 걸린다 (c) 한 번의 큰 손실 복구가 어렵다. 따라서 **거래 횟수는 적게, 대상은 유동성 높은 대형 코인으로 좁게, 손절은 기계적으로**.

### 5.1 1차 전략: 다코인 변동성 돌파 (래리 윌리엄스 방식)
- 매일 00:00 KST(빗썸 일봉 기준) 리셋.
- 목표가 = 당일 시가 + (전일 고가 − 전일 저가) × k (k=0.5 시작, 코인별 튜닝).
- 현재가가 목표가 돌파 시 시장가 매수 → 다음 리셋 시각에 전량 매도, 개별 −5% 즉시 손절.
- 대상: **BTC, ETH, XRP, SOL, DOGE, ADA 6종** (빗썸 KRW 마켓 유동성 상위). 코인별 하루 1회 진입이므로 하루 최대 ~5회 매수가 자연스럽게 나온다 (사용자 요청 반영).
- 추세 필터: 당일 시가가 5일 이동평균 위일 때만 진입 — 하락장 진입을 걸러낸다.

### 5.2 2차 전략(잔고 30만원 이상 도달 시 병행): 분할 DCA + 목표수익 매도
- BTC/ETH 한정, 하락 시 분할 매수 → 평단 대비 목표 수익률 도달 시 분할 매도.
- 횡보장에서 변동성 돌파의 약점을 보완. 분할 단위가 최소 주문 금액을 넘는 잔고가 됐을 때만 활성화.

### 5.3 Claude의 역할 (전략 튜닝 루프, 1시간~1일 주기)
- 최근 체결 로그와 시장 데이터(변동성, 추세 강도)를 분석.
- k값, 대상 코인 목록, 포지션 크기, 전략 on/off를 `config.yaml`로 조정.
- 조정 내역과 근거를 커밋 메시지 + 일일 리포트로 기록.
- 하락장 판단 시(예: BTC가 주요 이동평균선 아래) **현금 보유 모드** 전환 — 소액 계좌에서 가장 중요한 방어 수단.

### 5.4 백테스트
- 빗썸 캔들 API(일봉/분봉)로 최근 2~3년 데이터 수집.
- 수수료(쿠폰 적용 0.04%×2) + 슬리피지(0.1% 가정) 반영한 백테스트 필수.
- 지표: 누적수익률, MDD(최대낙폭), 승률, 손익비. **MDD > 20% 전략은 실전 투입 금지.**

---

## 6. 리스크 관리 (엔진에 하드코딩)

사용자의 하이리스크 성향을 반영해 한도를 완화하되, 완전 무제한은 두지 않는다 (원금 전멸은 복구 불가).

| 장치 | 규칙 (하이리스크 설정값) |
|------|---------------|
| 1회 주문 한도 | 총 운용자산의 18% (6코인 중 최대 5개 동시 보유 가능) |
| 동시 보유 | 최대 5코인, 일일 매수 최대 6회 |
| 개별 포지션 손절 | 진입가 대비 −5% 즉시 시장가 손절 |
| 일일 손실 한도 | −5% 도달 시 당일 신규 매수 전면 중단 |
| 누적 손실 킬스위치 | 총자산 고점 대비 −20% 도달 시 전량 매도 후 엔진 정지, 사용자 알림 |
| 주문 검증 | 주문 전 잔고·최소주문금액 재확인, 체결 후 실제 체결가 기록 |
| API 장애 대응 | 연속 실패 5회 시 신규 주문 중단, 재시도는 지수 백오프 |
| 이상 급변동 | 5분 내 ±10% 급변 시 신규 진입 금지 (이벤트 방어) |

---

## 7. 모니터링 & 알림

- **로그**: 모든 주문/체결/판단을 SQLite + JSON 로그로 기록, 매일 git 커밋(키 제외).
- **일일 리포트**: Claude가 매일 아침 수익률·보유현황·전략 판단 근거를 요약해 기록 (텔레그램 봇 연동 시 푸시 가능).
- **긴급 알림**: 킬스위치 발동, API 오류 지속, 잔고 불일치 시 즉시 알림 (텔레그램 권장 — 무료, 구현 간단).
- **사용자 수동 개입 스위치**: `STOP` 파일 하나 만들면 엔진이 즉시 신규 주문을 멈추는 단순한 정지 장치.

---

## 8. 단계별 로드맵

| 단계 | 내용 | 기간 | 통과 기준 |
|------|------|------|-----------|
| 0 | 사용자: 빗썸 API 키 발급(조회+주문, IP 등록), 수수료 쿠폰 적용, VPS 준비 | 1일 | 키·서버·쿠폰 준비 완료 |
| 1 | 엔진 뼈대 구축: 빗썸 클라이언트, 시세 수신, 잔고 조회, 주문 모듈, 로그 | 2~3일 | 수동 테스트 주문 1건 성공 |
| 2 | 백테스트: 변동성 돌파 전략 파라미터 탐색 (BTC/ETH) | 2~3일 | MDD<20%, 수수료 반영 후 + 수익 |
| 3 | **페이퍼 트레이딩**: 실주문 없이 가상 체결로 2주 운영 | 2주 | 백테스트 대비 괴리 원인 설명 가능 |
| 4 | **소액 실전**: 최소 주문 금액 수준으로 2주 | 2주 | 킬스위치·알림 정상 작동 확인 |
| 5 | 전액 투입 + Claude 튜닝 루프 가동, 월 단위 성과 리뷰 | 이후 | 월간 리뷰에서 시스템 유지/수정 결정 |

---

## 9. 사용자가 해야 할 일 (체크리스트)

- [ ] 빗썸 → 마이페이지 → API 관리 → API 2.0 키 발급 (**자산조회 + 주문만 허용, 출금 제외**)
- [ ] **수수료 무료(0.04%) 쿠폰 적용** — 미적용 시 0.25%로 전략 수익성이 무너짐
- [ ] VPS 계약 후 고정 IP를 API 키에 등록
- [ ] 현재 빗썸 원화 잔고 확인 (전략 파라미터를 실제 잔고 규모에 맞추기 위해 대략적인 금액을 알려줄 것)
- [ ] 텔레그램 알림 받을 봇 생성 (선택)
- [ ] 킬스위치 발동 시 연락 받으면 상황 확인

---

## 10. 유의사항 (법·세금·현실적 한계)

- **세금**: 국내 가상자산 투자소득 과세 시행 일정을 확인할 것 (유예 연장 이력 있음). 모든 거래 기록이 남으므로 신고 자료로 활용 가능.
- **약관**: 빗썸도 개인의 API 자동매매 자체는 허용. 단 초당 요청 제한을 준수해야 하며 위반 시 키가 차단될 수 있다.
- **현실적 기대치**: "큰 이익"은 결과이지 목표로 강제할 수 있는 값이 아니다. 소액 계좌가 커지는 경로는 (1) 손실을 작게 유지하고 (2) 검증된 우위를 반복하고 (3) 복리로 굴리는 것뿐이다. 월 단위로 시장 수익률(단순 보유) 대비 성과를 비교해 시스템이 실제 우위가 있는지 냉정하게 판정한다.
- **본인 명의**: API 키와 계좌는 반드시 사용자 본인 명의로만 운용한다.

---

## 11. 다음 단계

1. 레포에 엔진 뼈대(`engine/`), 빗썸 클라이언트(`exchange/`), 전략(`strategies/`), 백테스트(`backtest/`), 설정(`config.yaml`) 구조로 코드 구현 시작.
2. 빗썸 시세 조회·백테스트는 API 키 없이도 가능하므로 즉시 개발 가능.
3. 실주문 모듈은 사용자의 API 키 준비 완료 후 최소 주문 금액으로 검증.
