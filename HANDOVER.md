# 인수인계서 — 빗썸 자동매매 시스템

> 새 Claude 세션이나 몇 달 뒤의 나 자신이 이 문서 하나로 전체를 파악하도록 쓴다.
> 최종 갱신: 2026-09-05

---

## 0. 새 세션에서 시작할 때

Claude에게 이렇게 말하면 된다:

> "빗썸 자동매매 시스템 운영 중이야. GitHub `sjidok750-creator/Trade`의
> `claude/auto-trading-plan-vqnxhb` 브랜치에서 HANDOVER.md 읽고 이어서 도와줘."

---

## 1. 한 줄 요약

**빗썸 원화마켓 6개 코인을 대상으로, 20~30일 추세를 주 1회 판단해
오르는 코인만 보유하고 꺾이면 현금화하는 무인 매매 시스템.**
**2026-08-28 21:11 KST 실전 전환 완료.** 시작 자산 1,000,039원(실제 잔고 기준선).
6코인 전부 추세 위여서 코인당 163,339원씩 전량 진입한 상태로 시작했다.

---

## 2. 접속 정보

| 항목 | 값 |
|------|-----|
| 서버 | Vultr Seoul, `64.176.224.92` (Ubuntu 24.04, $5/월) |
| 접속 | `ssh -i ~/.ssh/vultr_trade root@64.176.224.92` (맥북에서) |
| 코드 위치 | 서버 `/root/Trade`, 원격 `sjidok750-creator/Trade` |
| 브랜치 | `claude/auto-trading-plan-vqnxhb` |
| 텔레그램 봇 | `sjidok_trade_bot` (챗 ID 8555400730) |
| 거래소 | 빗썸 (서브 계좌, 실전 운용 1,000,039원 / API 키 IP `64.176.224.92`, 출금 권한 없음) |

**중요**: SSH 개인키는 사용자의 맥북 `~/.ssh/vultr_trade` 에만 있다.
Claude는 서버에 직접 접속할 수 없고, GitHub 푸시 → 서버 자동 업데이트로만 반영한다.

---

## 3. 왜 이 전략인가 (결정 이력)

이 부분이 이 문서에서 가장 중요하다. 같은 실수를 반복하지 않기 위한 기록이다.

### 3.1 1차 전략(변동성 돌파)은 백테스트에서 탈락했다

실데이터 3년 백테스트 결과 k값 0.3~0.8 전 구간에서 **−51.8% ~ −86.3%**, MDD 57~86%.

**원인**: 3년간 1,817회 거래. 거래당 비용(수수료 0.08% + 슬리피지 0.2%)이
자산의 0.05%씩 갉아먹어 **비용만으로 약 −60%**. 전략 자체는 본전 근처였으나
비용이 전부 먹었다.

**교훈**: 소액 계좌에서 거래 빈도는 곧 손실이다. 저빈도가 1원칙.

### 3.2 저빈도 후보 비교에서 MA 추세추종이 승리

동일 조건(주 1회 리밸런싱) 비교 — 2023-08 ~ 2026-08:

| 전략 | 총수익 | MDD | 샤프 |
|------|--------|-----|------|
| BuyHold-6 (기준선) | +162.1% | 69.3% | 0.85 |
| BTC-Only (기준선) | +199.5% | 50.2% | 1.12 |
| **MA20-Trend** | **+247.6%** | **36.4%** | **1.36** |
| Momentum-Top2 | +181.6% | 54.6% | 0.90 |
| Mom+MA | +205.8% | 34.4% | 1.09 |

### 3.3 강건성 검증 통과 (과최적화 아님)

MA기간 {10,20,30,50,100} × 밴드 {0,1,3%} × 리밸주기 {7,3}일 = 30개 조합 중
**MA100을 제외한 거의 전부가 기준선을 상회**. 특정 숫자에 우연히 맞은 것이 아니라
"추세를 따르라"는 원리 자체가 이 시장에서 작동함을 확인.

**채택**: MA30 / 밴드 3% / 주 1회 → +292.2%, MDD 36.8%, 샤프 1.43, 비용 12.1%

### 3.4 계좌 목적: 이익확정 (업비트와 역할 분담)

사용자는 빗썸을 **이익확정** 계좌로, 업비트를 **자산증식** 계좌로 나눠 쓴다.
따라서 빗썸에는 `settle.target_krw`(기본 2만원) 단위로 이익을 현금 분리해
출금을 안내하는 규칙이 들어 있다.

검토 과정에서 두 방식을 비교했다:
- **A. 익절 후 재진입** (목표 도달 시 전량 매도 후 재진입) — 채택하지 않음.
  돈이 실제로 계좌 밖으로 나가지 않아 확정이 안 되면서, 상승 여력만 목표치에서
  잘리고 하락은 그대로 열린다. 추세추종의 수익 구조(소수의 큰 상승)를 깬다.
- **B. 이익만 분리 후 출금** — 채택. 원금은 계속 굴리고 이익분만 비례 축소해
  현금화하므로 전략이 온전하고, 출금된 돈은 실제로 확정된다.

### 3.5 리스크 한도를 전략에 맞춰 재조정

전략의 역사적 MDD가 37%이므로 기존 킬스위치 −20%는 정상 운영 중에도
발동해버린다. 따라서 **킬스위치 −40%** (재난 브레이크), 개별 손절 −15%
(급락 방어)로 조정. 주 출구는 손절이 아니라 **추세 이탈(MA−3%)** 이다.

---

## 4. 시스템 구조

```
exchange/bithumb.py   빗썸 API 2.0 클라이언트 (JWT, 출금 기능 없음)
strategies/ma_trend.py  추세 상태 판단 (MA ± 밴드 이력대)
engine/runner.py      24시간 엔진: 시세 폴링, 주간 리밸런싱, 리스크 가드
engine/risk.py        손절·일일한도·킬스위치 강제
engine/report.py      KST 08~22시 2시간마다 현황 리포트
engine/weekly.py      주간 요약 출력 (사용자가 복붙 → Claude 리뷰)
engine/fee_guard.py   빗썸 0.04% 쿠폰 30일 만료 감지
engine/notify.py      텔레그램 발송 + 챗ID 자동 조회
backtest/portfolio.py 비중 기반 백테스터 (전략 공정 비교용)
backtest/lab.py       전략 연구실 (후보 비교 + 강건성 스윕)
autoupdate.sh         10분마다 GitHub 확인 → 테스트 통과 시 자동 반영
config.yaml           전략·리스크 설정 (Claude가 튜닝 시 여기를 수정)
```

**서비스 2개가 상시 가동:**
- `trade.service` — 매매 엔진
- `autoupdate.timer` — 10분 주기 자동 업데이트

---

## 5. 자주 쓰는 명령

```bash
systemctl status trade              # 엔진 상태
journalctl -u trade -f              # 실시간 로그
tail -20 ~/Trade/logs/events.jsonl  # 거래·판단 기록
cat ~/Trade/state/trend.json        # 코인별 추세 판단 상태
cat ~/Trade/state/positions.json    # 현재 보유

touch ~/Trade/STOP                  # 긴급: 신규 매수 중단 (손절은 계속 동작)
rm ~/Trade/STOP                     # 해제
systemctl stop trade                # 엔진 완전 정지

.venv/bin/python -m backtest.lab            # 전략 후보 재비교
.venv/bin/python -m backtest.lab robust     # 강건성 재검증
```

---

## 6. 안전 장치

| 장치 | 값 | 비고 |
|------|-----|------|
| 출금 권한 | **없음** | API 키에 미부여 — 최후 방어선 |
| IP 제한 | `64.176.224.92` | 빗썸에 등록된 IP에서만 주문 가능 |
| 개별 손절 | −15% | 급락 방어 (주 출구는 추세 이탈) |
| 일일 손실 한도 | −8% | 당일 신규 매수 중단 |
| 킬스위치 | 고점 대비 −40% | 전량 매도 후 엔진 자동 정지 |
| 급변동 가드 | 5분 내 ±10% | 신규 진입 금지 |
| 수동 정지 | `touch STOP` | 파일 하나로 즉시 중단 |
| 확정 시 낙폭 보정 | `risk.withdraw()` | 출금을 손실로 오인해 킬스위치가 발동하는 것을 방지 |
| 수수료 쿠폰 만료 | 3일 전 경고 + 체결 수수료 역산 | 0.25% 복귀 시 감지 |
| 자동 업데이트 안전판 | 테스트 17건 통과 필수 | 실패 시 이전 버전 유지 |

**최종 통제권**: 무슨 일이 있어도 빗썸에서 API 키를 삭제하면 시스템은 아무것도 못 한다.

---

## 7. 진행 상태와 다음 단계

- [x] 서버 구축 (SSH 키 인증)
- [x] 전략 연구 및 검증
- [x] 엔진 구현, 테스트 17건
- [x] 페이퍼 트레이딩 가동
- [x] 텔레그램 알림 + 정기 리포트
- [x] 자동 업데이트
- [x] 페이퍼 관찰 (8/23~8/28)
- [x] 빗썸 API 키 발급 (IP `64.176.224.92`, 자산조회+주문조회+주문하기, **출금 제외**)
- [x] 실전 전환 2026-08-28 (`bash golive.sh`, 사전점검 전 항목 통과)
- [ ] **실전 검증기 1~2개월** ← 현재 여기. 월간 BuyHold 비교로 우위 판정
- [ ] 우위 확인 후: 자본 증액(100→300만) → 유니버스 확장·BTC 레짐 필터 백테스트
- [ ] 쿠폰 재신청 ~9/21 (8/22 신청, 30일). 재신청 후 `fee.coupon_renewed_on` 갱신
- [ ] 텔레그램 봇 토큰 회전 (스크린샷 노출) — BotFather `/revoke` → `.env` → `systemctl restart trade`

### 실전 1주차 확인 사항 (2026-09-05)

- 체결 슬리피지: BTC +0.04 / XRP +0.09 / SOL −0.10 / ADA −0.31 / **DOGE −0.79%**,
  평균 −0.21% → 백테스트 가정(편도 0.28%) 안쪽. DOGE 호가가 얇다 — 유니버스
  확장 시 얇은 코인은 지정가 진입 검토.
- 수수료 0.04% 실제 적용 확인. 기존 보유 ETH 0.218개는 시스템이 건드리지 않음.
- 9/4 00:00 첫 주간 리밸런싱 정상 (6코인 전부 추세 유지, 매매 없음).
- 9/2 15:49 KST 엔진 1분 내 2회 재시작 — 원인 미확인(journalctl 확인 필요).
  재시작 시 `new_day` 중복 실행 문제는 코드로 막음(같은 거래일이면 건너뜀).

### 실전 전환 조건 (스스로 지킬 것)

1. 페이퍼 1~2주간 엔진 무중단, 오류 로그 없음
2. 실제 체결 판단이 백테스트 로직과 일치 (리밸런싱 시점·대상 검증)
3. 킬스위치·손절·STOP 파일이 실제로 작동함을 확인
4. **사용자의 명시적 승인**

### 운영 리듬

- **주간**: 사용자가 서버에서 `.venv/bin/python -m engine.weekly` 출력을 붙여넣으면
  Claude가 리뷰한다 (시장 상황 웹검색 포함). 필요 시 파라미터 조정 → 백테스트 재검증 → 커밋
- **월간**: 실성과 vs BuyHold 기준선 비교. **우위가 사라지면 전략 교체를 논의한다.**
- 절대 금지: 검증 없는 즉흥 파라미터 변경. 그것은 감정 매매의 자동화일 뿐이다.

---

## 8. 알아둘 함정

1. **빗썸 수수료 쿠폰은 30일마다 재신청**해야 한다. 놓치면 0.25%로 복귀해
   전략 수익성이 무너진다. 재신청 후 `config.yaml`의 `fee.coupon_renewed_on` 갱신.
2. **Vultr 로그인은 구글 계정으로만** 된다 (`my.vultr.com/login` → Google 버튼).
   이메일+비밀번호는 존재하지 않는다.
3. **Ubuntu 이미지는 root 비밀번호 SSH 로그인을 막아둔다.** 서버를 새로 만들면
   반드시 SSH 키를 등록해서 배포할 것. (이걸 몰라서 하루를 날렸다.)
4. **백테스트 수치를 미래 수익으로 착각하지 말 것.** 2023~2026은 코인 상승장이
   포함된 구간이다. 하락장에서는 현금 비중이 늘며 수익이 나지 않는 게 정상이다.
   이 전략의 강점은 수익률이 아니라 **낙폭 절반**이다.
